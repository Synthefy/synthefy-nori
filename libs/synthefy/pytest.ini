[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short -m "not slow"
markers =
    slow: opt-in tests that may load local models or external artifacts
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
